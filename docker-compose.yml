version: "3.8"
services:
  acol-dev:
    build:
      context: .
      dockerfile: Dockerfile.prod
    volumes:
      - ./src:/app/src
    ports:
      - 3000:4080
    networks:
      - reverse-proxy

networks:
  reverse-proxy:
    external: true
